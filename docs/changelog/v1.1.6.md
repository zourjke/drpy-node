---
date: 2025-01-10
type: patch
title: V1.1.6 版本更新
---

## 其他

- 更新 `虎斑[盘].js` 域名
- `req` 底层请求函数优化，确保错误返回时可以取到返回的html内容
- `火车太堵`原域名访问有cf验证了无法获取源码没法修了，现在更换一个域名
- 修复示例代码 `360test.js` 运行错误
- `moduleExt` 增加默认值为空字符串而不是 `undefined`
- 不允许在源或者解析里写 `const axios = require('axios');` 会出现无法解决的问题
- 增加一个密源
- 尝试修复解析 `虾米.js` 以失败告终，解出来ip开头的地址还是无法播放
- 增加 `config/parses.conf` 用于手动配置web解析和json解析
- 更新所有网盘源的搜索，为套娃党的粗心擦屁股
- 已知bug:
- 装逼壳搜索不支持传参源，采王系列无法获取结果，easybox正常
