---
date: 2024-12-31
type: patch
title: V1.0.27 版本更新
---

## 其他

- 设置中心优化，样式微调
- 新增 `_lib.waf.js` 通用过长城雷池防火墙工具，与对应示例源 `团长资源[盘].js`
- 优化 `多多[盘].js` 默认筛选不正确导致没数据问题
- 新增源 `专享影视.js` `火车太堵.js`
- 增加 `robots.txt` 防止被引擎收录
- 服务启动增加打印nodejs版本号
- 主页接口增加basic验证，请自己手动配置.env文件中的 `API_AUTH_NAME` 和 `API_AUTH_CODE`
- 配置接口和源接口增加api授权，在.env文件中配置 `API_PWD = dzyyds`
- AI工具集成到 `AIS` 对象里了
- ENV环境变量get和set方法增加参数3:`isObject=1`,支持读写变量如果是字符串自动转为object对象
- AI库完成讯飞星火智能体对接，可配置当前AI为3，新增依赖库 `ws`
