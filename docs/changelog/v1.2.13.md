---
date: 2025-08-17
type: patch
title: V1.2.13 版本更新
---

## 其他

- 支持hipy源T4,参考[python环境](/docs/pyenv.md) 进行python依赖安装。
- 需要保证终端输入 `python` 能正常识别到即可(保证本地安装了python并且有环境变量)
- 增加 `hipy` 适配器
- 新增依赖 `python-shell` 需要手动安装
- 统一libs接口 `cate` 改为 `category`
- 修复了 定时任务 `execute-now` 接口返回数据错误
- 目前py源的T4支持 ext扩展、getDependence依赖、以及 本地代理 和 动作，凑合能用，代码实现很狗屎。
